import { PassiveData } from "../passive";

export const CommandersScar: PassiveData = {
    name: "Commander's Scar",
    description: "The unit applies `Scar` status on hit.",
    callbacks: {}, // Logic for applying status effect is likely handled by the attack system or status effect system itself, triggered by a tag or property on the unit/attack.
};
